const { ENUM_FILE_TYPE } = require('../generic/enums')
const fs = require('fs');
/**
 * 
 * @param {*} data 
 * @param {Object} args { 
 * type: ENUM_FILE_TYPE 
 * id: string
 * }
 */
module.exports.save = (data, args) => {
    try {
        
        
    } catch (e) {

    }
} 

module.exports.load = (args) => {

}