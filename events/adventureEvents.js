const eventBuilder = require('../build/event')
const { ENUM_EVENT_TYPE } = require('../generic/enums')

/**
 * 
 * @param {Object} world 
 * @param {Object} output logger object 
 */
 const dailyEvent = (world, output) => {

}